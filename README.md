# Mark Anthony Nene Portfolio · Astro Edition

This package is prepared for deployment to the `Rhyxeee.github.io` user-site repository.

Included updates:
- Astro + Tailwind structure
- live user-site config for `https://rhyxeee.github.io`
- featured projects carousel with hover arrows, dots, keyboard, and swipe support
- imported project images in `public/images`
- homepage already wired to `ProjectsCarousel.astro`

Note:
- project and experience PDF buttons currently point to the existing GitHub repo file URLs so they continue to work without extra PDF migration.
- if you later want every PDF served directly from the site domain, move those PDFs into `public/docs` and update the links.
